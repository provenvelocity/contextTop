# Tasks

## In Progress

- [ ] 2026-09-06 — **Emit `fix_verified`** — Engine-owned verification of an applied fix against the first post-apply request snapshot, per the comparison rules in `docs/PRODUCT.md#contexttop-fix` (same `model_id`, unchanged `unknown_source_count`, delta limited to frozen `targetSourceKeys`). `reportLifecycle` handles proposed/accepted/applied today; verified is the remaining piece.
- [ ] 2026-09-06 — **Close the diagnostics validation gate** — Complete the checklist in `docs/arch/SIGNALS.md#diagnostics-validation-gate` (OTLP schema validated + field-allowlisted per platform, raw trace fallback pinned, cross-platform probe on 5+ requests, per-signal coverage, VS Code/Copilot version matrix, no-raw-content tests) before diagnostics are on by default. A first-cut opt-in tailer already ships; this formalizes it.
- [ ] 2026-09-05 — **Global uninstall of unneeded VS Code extensions** — `.vscode/extensions.json` recommendations + unwantedRecommendations are in place (non-destructive, reversible). Global uninstall remains pending explicit user confirmation.

## Completed

- [x] 2026-09-07 — ci: quote if-expressions in ci.yml (colon in 'release: v' broke YAML parsing, disabling all CI runs)
- [x] 2026-09-07 — fix: remove JSONC comments from .vscode/extensions.json for strict-JSON validators
- [x] 2026-09-07 — ci: auto-generate AI release notes from commits + TASKS.md between tags (GitHub Models, zero-secret, deterministic fallback)
- [x] 2026-09-07 — ci: skip CI on the release-bump commit to avoid a duplicate run alongside Release
- [x] 2026-09-07 — feat: first testable release
- [x] 2026-09-07 — feat: push.sh auto-cuts SemVer releases from Conventional Commits (0.x-aware); --release/--no-release flags
- [x] 2026-09-07 — **README rewrite for quick use** — Rewrote the root README as a VS Code extension landing page (tagline, Features, Requirements, Install prebuilt/from-source, Quick start, Settings table, Commands table, Privacy, Documentation split, Contributing, License) focused on getting users running fast.
- [x] 2026-09-07 — **Move architectural docs to docs/arch** — `git mv` ARCHITECTURE, SIGNALS, IPC, METRICS, IMPLEMENTATION_PLAN into `docs/arch/`; rewrote all cross-doc links and updated `scripts/check-docs.py` to recurse (rglob).
- [x] 2026-09-07 — **Deepen docs navigation** — Added `docs/README.md` and `docs/arch/README.md` index pages splitting product/usage docs from technical specs; kept product docs (PRODUCT, DASHBOARD, TOOL_STORY, TESTING) in `docs/`.
- [x] 2026-09-07 — **Repoint moved-spec references** — Updated `docs/arch/` paths in Rust source doc comments (engine, lib, model, protocol, recommend, source_key, tokenizer) and the live TASKS.md links; docs link gate passes 0 broken / 0 whitespace.
- [x] 2026-09-07 — ci: cross-compile darwin-x64 on macos-14 (drop stuck macos-13 runner); clearer install docs
- [x] 2026-09-07 — push.sh: no-arg mode always auto-generates a message (In Progress todos, else changed-file summary)
- [x] 2026-09-07 — push.sh: derive commit message from In Progress TODO statements when no message is given
- [x] 2026-09-07 — chore: add push.sh (commit+TODO update+push as one unit) and TODO commit instruction
- [x] 2026-09-06 — **Fix lifecycle: `request.reportLifecycle` (proposed/accepted/applied)** — Add the `ReportLifecycleRequest`/`LifecycleResponse` protocol types and an engine handler that registers `fix_proposed` on first ranking, validates adapter `fix_accepted`/`fix_applied` transitions (correct order; `removedSourceKeys` only on apply and within frozen `targetSourceKeys`; engine-owned kinds rejected; tool events require `operationId`), and acks with the new fix state. Adapter reports `fix_accepted` when the user acts on the Story card's *Manage tools…*. 6 new engine tests; `fix_verified` scoped as the remaining piece. Consistent with `docs/IPC.md#requestreportlifecycle`.
- [x] 2026-09-06 — **Extension recommendations file** — `.vscode/extensions.json` lists the stack's recommended extensions and steers VS Code to disable unrelated stacks for this workspace (reversible; nothing uninstalled).

- [x] 2026-09-06 — **First wrap-up: CI + testing docs + clean baseline** — Add `.github/workflows/ci.yml` (Rust fmt/clippy `-D warnings`/test/build engine artifact, extension `npm ci`+compile, docs check), `scripts/check-docs.py` (broken-link + trailing-whitespace gate), and `docs/TESTING.md` (build/run-F5/try/CI-parity). Normalize Rust formatting and fix the `collapsible_if` clippy lint so the baseline is fmt- and clippy-clean. README gains build/run/test + repo-map links. Tag `v0.1.0` as the wrap-up point.

- [x] 2026-09-06 — **Wire tool story through engine `unselect_tools` recommendation** — Correct the engine Tools rule to `execution: guided` (no API disables tools; matches PRODUCT/IPC), add `tool_unselect_is_guided` test, and have the adapter call `request.getRecommendations` and render the ranked `unselect_tools` item (engine-computed savings range, `fixId`, `targetSourceKeys`, guided) in the Story card. Remaining open: fix lifecycle events blocked on `request.reportLifecycle` in the engine binary.

- [x] 2026-09-06 — **Write up the tool story (cross-referenced)** — Add `docs/TOOL_STORY.md` documenting the tool-efficiency story + per-call toggling feasibility, cross-referenced to SIGNALS/METRICS/PRODUCT/IPC/ARCHITECTURE/DASHBOARD, confirming consistency with the frozen `unselect_tools` guided-fix contract and flagging the open item (migrate adapter-side Story to an engine `getRecommendations` item).
- [x] 2026-09-06 — **Tool-efficiency story card + prune action** — Add a Story card that narrates tool context cost: joins per-tool schema cost (`vscode.lm.tools`) with tools Copilot actually invoked (`tool_call` spans), headlines total tool tokens + % of last request, lists biggest unused tools with token savings, and a "Manage tools…" action that opens VS Code tool config or guides to the Chat Tools picker. Documented per-call AI-toggling feasibility (recommend-only; no API to disable tools programmatically). New `story` card in layout + `contextTop.manageTools` command.

- [x] 2026-09-06 — **Chart metric selector + all-metrics overlay** — Add a Request-mode dropdown to plot All metrics (default, each normalized to its own max), Composition, or a single per-request metric in real units (input / budget % / cache hit % / uncached / context growth / TTFT / latency / output / billing). Chart legend shows live per-metric values; picking a metric switches to Request mode.
- [x] 2026-09-06 — **Fix F5 crash: harden engine process error handling** — Add missing `error` handlers on the engine `ChildProcess` and its stdin/stdout/stderr streams (unhandled EPIPE/ENOENT was throwing uncaught and crashing the extension host), guard writes against a dead/unwritable engine, add `export function deactivate()` to kill the engine on host reload (prevents orphan buildup across F5), and stop the engine output channel from stealing focus.

- [x] 2026-09-06 — **Request chart: one line per context item + auto-switch** — Draw request mode as a colored line per source item (system prompt / tools / prompt / other) over time with per-request dots and a model budget line (red dot when over budget). Auto-switch to Request mode on the first observed request so the multi-line view is the default; manual toggle disables auto-switch.
- [x] 2026-09-06 — **Request chart as stacked columns vs budget** — Render request mode as one stacked column per observed request (system prompt / tools / prompt / other) scaled against the model budget line, with over-budget columns outlined red, so bad context choices are visible over time. Draws from a single request; column width adapts to request spacing.
- [x] 2026-09-06 — **Chart mode: Candidate vs Request composition** — Add a chart toggle so the line chart can plot per-request composition (system prompt / tools / prompt / other) as multiple colored lines from the sidecar decomposition, separate from the ambient candidate gauge. Candidate thresholds/zones apply only in candidate mode; header + status pill relabel per mode. Data-driven via the existing `SERIES` palette.
- [x] 2026-09-06 — **Fully implement docs/DASHBOARD.md** — §2.1 remaining per-request metrics (temperature/topP/billing/responseId); §2.2 per-turn/session (turn duration, requests/turn, tool-calls/turn, session duration, Copilot/VS Code version); §2.3 tool metrics surfaced; §2.4 discovery hidden-context counts + resolve latency; §2.6 correlations/anomalies rendered; §3 reconciled to request-scoped decomposition; §4.2 extensible card model (`data-card` + `contextTop.dashboardLayout`), `SERIES` chart abstraction, and typed `DashboardEvent` contract (`dashboardContract.ts`). Verified parsing against real logs; compile clean.
- [x] 2026-09-06 — **Implement sidecar decomposition + per-request metrics** — Tail `llm_request` sidecars (`systemPromptFile`, `toolsFile`) + `models.json`; add TTFT, budget %, message count, operation name, and a stacked system-prompt/tools/prompt/other request-context breakdown (normalized so estimates never overflow). TS-only, no engine/candidate-gauge changes; buffers tokenized then dropped.
- [x] 2026-09-06 — **Write up dashboard/metrics/sidecar plan** — Documented new metrics, sidecar decomposition, and dashboard extensibility fixes in `docs/DASHBOARD.md`, including the honest answer that the current dashboard is a bespoke (non-extensible) canvas renderer.
- [x] 2026-09-05 — **Phase 2: redaction rule setting** — Add bounded declarative `contextTop.redactionRules` configuration that cannot weaken built-in secret/path redaction.
- [x] 2026-09-05 — **Phase 2: tokenizer mapping setting** — Add bounded declarative `contextTop.modelTokenizerMappings` configuration and matching `setConfig` handling.
- [x] 2026-09-05 — **Phase 2: tool-result accounting** — Correlate tool inventory, invocations, and result-token measurements with request lifecycle while preserving candidate/confirmed distinctions.
- [x] 2026-09-05 — **Phase 2: validated diagnostics adapter** — Ship diagnostic ingestion only after the validation gate, with pinned filenames, extension IDs, VS Code/Copilot versions, strict allowlists, deduplication, and parser-health metrics.
- [x] 2026-09-05 — **Phase 2: session handoff summaries** — Add guided or participant-owned clean-chat handoff summaries without silently rewriting Copilot conversations.
- [x] 2026-09-05 — **Phase 2: `@contexttop /fix` workflow** — Read only participant-visible prompt/references/tools/model, record its confirmed snapshot, request ranked fixes, and stream preflight guidance.
- [x] 2026-09-05 — **Phase 1: `@contexttop` confirmed snapshots (adapter)** — Wire `request.recordRequestSnapshot` for participant-visible evidence; ambient file/tool/terminal/instruction observations stay candidate unless the participant explicitly includes them. Diagnostic aggregate request totals remain separate until per-source composition is validated.
- [x] 2026-09-05 — **Phase 1: bounded SQLite storage** — Persist sampling (≤1 candidate row/source/5s), migrations, age/size cleanup, hourly rollups, no-raw-content tests. Engine currently holds candidate gauge + snapshots in memory only.
- [x] 2026-09-05 — **Instructions audit surface rendering** — Render the collected `instrFiles` array in the webview: detail expandable or table view showing per-file byte sizes and recentness.
- [x] 2026-09-05 — **Tools audit surface rendering** — Render the collected `topTools` array in the webview: detail expandable or table view showing per-tool token estimates and whether the tool was invoked in the last observation window.
- [x] 2026-09-05 — **Diagnostics validation gate: evaluate Agent Debug Log / OTLP export** — Validate `github.copilot.chat.agentDebugLog.fileLogging.enabled` OTLP JSON schema (system prompt, resolved context, tool payloads, token summary) as the preferred, structured diagnostic source over raw trace-log scraping; re-audit which `SIGNALS.md` rows can move off `unknown` for standard chat. Gate item, not shipped.
- [x] 2026-09-05 — **Phase 1: F5 manual verification (run-to-testable #7)** — After Phase 1 correctness blockers are fixed, launch the extension with F5; verify engine handshake, candidate-only chart behavior, separate observed request metrics, source removal, runtime diagnostic controls, CSP/no-raw-content guarantees, timeline/replay recovery, and reversible fix flow.
- [x] 2026-09-05 — **Reconcile planning docs with current product decisions** — Remove stale status-bar requirements, stop calling structured session JSONL validated OTLP, align diagnostics gating with implementation, and make Phase 1 prerequisites consistent across the implementation and kickoff plans.
- [x] 2026-09-05 — **Harden analytics sampling and rendering** — Batch updates, bound dimension cardinality, expose retained sample/time windows, mark truncation partial, and suppress unstable tail/correlation statistics below minimum sample sizes.
- [x] 2026-09-05 — **Advertise only implemented capabilities** — Remove `signals.terminalShellExecution` and `signals.participant` from hello until real collectors are active; advertise diagnostics only for a validated parser.
- [x] 2026-09-05 — **Fix transient-content wire safety** — Send camelCase `transientContent`, enforce the documented 64 KiB decoded UTF-8/ANSI-stripped bound, mark truncation partial, and test that raw content never reaches logs or persistence.
- [x] 2026-09-05 — **Wire recommendations and reversible fix flow** — Call `getRecommendations`, render proposed fixes, require confirmation, execute adapter-owned actions only, and report accepted/applied/verified lifecycle outcomes.
- [x] 2026-09-05 — **Package the engine binary** — Resolve the host-architecture executable from the packaged extension instead of relying on `target/debug/engine`; add unavailable-state handling and packaging tests.
- [x] 2026-09-05 — **Complete timeline, lifecycle, and replay IPC** — Implement real historical buckets, `request.reportLifecycle`, sequenced subscriptions, bounded replay, `event.streamGap`, and extension-side subscribe/resync behavior.
- [x] 2026-09-05 — **Rotate session identity on engine restart** — Allocate a new session ID and nonce for every child spawn, then restore configuration and subscriptions without reusing a session ID with a new HMAC key.
- [x] 2026-09-05 — **Fix diagnostic runtime lifecycle and consent** — Use one disclosure-driven flow for producer/consumer settings and dynamically start or stop the tailer without requiring reload.
- [x] 2026-09-05 — **Enforce capture level off** — Stop collectors and diagnostic tailing, reject or ignore ingest, clear live state, and purge future persisted data when `captureLevel` is `off`.
- [x] 2026-09-05 — **Add candidate source lifecycle** — Define removal/tombstone and expiry semantics, then reconcile closed files, removed tools, deleted instructions, and ephemeral tool results out of the live gauge.
- [x] 2026-09-05 — **Separate request totals from candidate pressure** — Stop ingesting diagnostic `llm_request.inputTokens` as a `prompt` candidate; add a dedicated observed request series/snapshot path so aggregate request input is never summed with ambient sources.


- [x] 2026-09-05 — **Reconcile remaining TODOs and plans** — Expanded the backlog to 24 atomic deliverables plus the Phase 2 milestone and documented the current state, Phase 1 gate, diagnostics gate, Phase 2 workflow, Phase 3 horizon, and stale-plan contradictions in `review-findings.md`.
- [x] 2026-09-05 — **Clean report Markdown** — Consolidated `review-findings.md` into one report with one H1, consistently formatted tables, zero editor diagnostics, zero broken local links, and clean whitespace.
- [x] 2026-09-05 — **Correct report terminology and priorities** — Replaced unsupported OTLP claims with structured-span terminology, clarified cohort and 500-sample scope, distinguished derived statistics from measurement labels, and reprioritized only current defects.
- [x] 2026-09-05 — **Preserve verified Copilot metrics research** — Retained the validated 4-session/8-request statistics, full 17-directory source inventory, evidence limitations, context-funnel unknowns, and privacy findings.
- [x] 2026-09-05 — **Rewrite findings as current-state report** — Replaced mixed historical audits with one authoritative review, active P0–P2 findings, implementation order, validation status, and a concise resolved-findings appendix.
- [x] 2026-09-05 — **Review consolidated findings report** — Verified `review-findings.md` against current code and specifications; identified obsolete appended audits, scope/statistical labeling ambiguities, overclassified risks, document lint failures, and the active request-vs-candidate correctness defect.
- [x] 2026-09-05 — **Recommend logging-system improvements** — Prioritized nine implementation steps across request/candidate separation, strict schema validation, deduplication, consent/privacy, metric quality labels, companion metadata, span timelines, bounded persistence, and upstream context-funnel instrumentation.
- [x] 2026-09-05 — **Validate logging behavior against local sessions** — Verified 17 directories across two roots, 94 events, 4 substantive request sessions, 35/35 resolved parent links, 8/8 valid companion references, zero malformed lines, companion sizes/counts/hashes, and producer versions without exposing raw content.
- [x] 2026-09-05 — **Audit currently captured log metrics** — Cataloged every ambient, diagnostic, derived, and unknown metric with its actual measurement/inclusion/coverage behavior; identified confirmed request totals currently mislabeled inside the candidate gauge.
- [x] 2026-09-05 — **Research Copilot diagnostic-log architecture** — Documented session and companion files, one-second newest-session tailing/backfill, legacy Hooks/Chat parsers, generic scanner behavior, engine ingestion, in-memory analytics, and webview flow in `review-findings.md`.
- [x] 2026-09-05 — **Document full analyst findings** — Added all 11 requested sections to `review-findings.md`: executive findings, metric definitions/formulas/priorities/dimensions/limitations, context funnel, latency, errors, correlations, anomalies, debugging opportunities, instrumentation, dashboard design, and next-level analysis.
- [x] 2026-09-05 — **Implement analyst dashboard panels** — Added current-session request, context, latency/TTFT, error/anomaly, correlation, model, operation, tool, and instrumentation-coverage panels. Unsupported fields render `Unknown`, and every distribution/correlation displays its sample size.
- [x] 2026-09-05 — **Implement error and anomaly metrics** — Added bounded request/tool outcome classification, privacy-safe error categories, explicit-only retry counting, cache-variability detection, and minimum-sample/two-standard-deviation latency anomaly gating.
- [x] 2026-09-05 — **Implement latency distribution metrics** — Added bounded min/p50/p75/p90/p95/p99/max/mean/population-standard-deviation distributions for request latency and TTFT, plus per-model/tool/operation p50 latency.
- [x] 2026-09-05 — **Implement context lifecycle metrics** — Added observed input/output/cached/uncached token distributions, cache-hit ratio, request-to-request context churn, and an explicit observed/unknown context-funnel coverage table; no relevance, truncation, or use is inferred.
- [x] 2026-09-05 — **Implement request outcome metrics** — Added request success/failure/cancellation/unknown counts, explicit retry count, turn/tool activity, and model/operation breakdowns from structured session spans.
- [x] 2026-09-05 — **Analyze current Copilot logs statistically** — Analyzed 4 substantive sessions (81 events, 8 requests, 8 tool calls) with full distributions, request timelines, session behavior, context churn, cache patterns, correlations, and observed-vs-unknown qualification; excluded the one-event session as insufficient data.
- [x] 2026-09-05 — **Add mouseover tooltip to chart** — When you hover over the chart, a tooltip appears showing the total tokens and per-source breakdown for that time point. Tooltip finds the nearest data point within 20px and displays it styled to match VS Code theme (dark widget background, rounded border, no pointer events).
- [x] 2026-09-05 — **Phase 1: redaction module** — Engine drops raw content after tokenizing; added bounded built-in redaction (SecretRule, PathRule) before optional detail storage. Redaction happens in engine's `derive_tokens()` before storing transient content. 7 new tests, all passing.
- [x] 2026-09-05 — **Phase 1: `setConfig` + policy revision** — Authoritative policy copy wired: engine accepts `request.setConfig` with the adapter's effective config, bumps `policyRevision` only when effective config changes. Ensures recommendation identity stays stable even with config churn. Added 2 integration tests (policy revision bumping, transient content redaction).
- [x] 2026-09-05 — **Tools audit data collection** — The extension's ambient collector now builds and streams a `topTools` array (top 12 by token estimate) with estimated tokens per loaded tool. Integrated into `AmbientInventory` and sent to the webview; invoked/unused classification remains pending because the ambient API does not provide it.
- [x] 2026-09-05 — **Instructions audit data collection** — Extension scans for instruction/prompt/agent files (`copilot-instructions.md`, `*.instructions.md`, `AGENTS.md`, `*.prompt.md`), builds `instrFiles` array (top 12 by byte size), integrated into `AmbientInventory` and sent to webview via `onMetrics` callback.

- [x] 2026-09-05 — **Time-based chart window (default 5 min, UI-settable)** — Added `contextTop.timeWindowMinutes` (default 5, enum 1/5/15/30/60). Rewrote the chart x-axis to be time-based (right edge = now, left = now − window), pruning points by time and scrolling once per second even when idle. Added in-panel window selector buttons (1m/5m/15m/1h+) with an active-state highlight that also writes the setting; axis label reflects the window.
- [x] 2026-09-05 — **Enable buttons reflect ON state** — The panel's "Enable diagnostic logs" / "Enable Copilot Agent Debug Log" buttons now show an active highlight and read "… : ON" when enabled. Provider streams effective enabled state (diagnostics counts the debug-host auto-on) and re-posts on config change.

- [x] 2026-09-05 — **Read Copilot's real per-request token usage (debug-logs/main.jsonl)** — Discovered the structured chat debug logs at `globalStorage/github.copilot-chat/debug-logs/<session>/` (`main.jsonl`, `system_prompt_0.json`, `tools_0.json`, `models.json`). `main.jsonl` is OTLP spans; the `type:"llm_request"` span carries **observed** `inputTokens` (full context sent, ~180k), `outputTokens`, `cachedTokens`, `maxTokens`, `model`. Repointed `DiagnosticLogTailer` (via `context.globalStorageUri`) to tail the newest session's `main.jsonl` and ingest each request's `inputTokens` as an observed request-context observation. This is the real Copilot context number the product exists to show.
- [x] 2026-09-05 — **Fix in-panel toggle buttons (were failing silently)** — Added logging to the webview message handler and both toggle commands; wrapped config writes in try/catch with surfaced errors; the OTLP command now falls back to writing the setting directly into user `settings.json` (or opening Settings) when `config.update` is rejected for an unregistered key. Added missing `writeUserSettingKey` helper.

- [x] 2026-09-05 — **Chart threshold bounds (amber/red zones)** — Added `contextTop.warnTokens` (40k) and `contextTop.criticalTokens` (80k) settings; the total line is drawn per-segment in its normal color below `warn`, amber in the warning zone, and red above `critical` (per-source lines keep their own colors). Dashed threshold guide lines and a status pill (OK / WARNING / CRITICAL) added; thresholds stream to the webview and update live on config change.
- [x] 2026-09-05 — **Auto-enable diagnostics in the debug host** — Diagnostic log ingestion now turns on automatically when `context.extensionMode === Development` (F5), so no manual setting is needed while debugging; still respects `contextTop.enableDiagnosticLogs` for installed use.
- [x] 2026-09-05 — **In-panel toggle buttons + first-run OTLP prompt** — Webview toolbar buttons ("Enable diagnostic logs", "Enable Copilot Agent Debug Log") postMessage to `contextTop.enableDiagnostics` / `contextTop.enableAgentDebugLog` commands (config.update Global), so users never open Settings. A once-per-install information prompt offers to enable the Agent Debug Log (OTLP).
- [x] 2026-09-05 — **OTLP discovery + data-catalog readiness** — `DiagnosticLogTailer` now discovers Agent Debug Log (OTLP) files once enabled, logs their structure, and defensively extracts well-known token-usage fields (ingested as observed `prompt`). Documented the full OTLP data catalog in `SIGNALS.md` and confirmed every data point maps to an existing engine `SourceKind`. Finalized OTLP field parsing is deferred until a real record is captured. 39 tests green, `tsc` clean.

- [x] 2026-09-05 — **Diagnostic log ingestion (opt-in): real Copilot activity → live observations** — Investigated the actual Copilot logs (`GitHub Copilot Chat Hooks.log`, `GitHub Copilot Chat.log`) in the current window's `exthost` dir. Built `DiagnosticLogTailer`: derives the Copilot log dir from `context.logUri` (sibling under `exthost/`), tails both files from EOF, and ingests (a) each `PostToolUse` agent tool call as a `tool_results` observation sized by `tool_response` bytes (estimated), and (b) `Summarization usage: prompt=…` lines as `history` observations with **observed** token counts. Gated behind `contextTop.enableDiagnosticLogs`. Added `ObservedCounts.tokenCount` + an `observed` measurement path in the engine so provider-reported token counts are labeled `observed`, not estimated. 39 tests green, `tsc` clean. — Updated `ARCHITECTURE.md` (current status now reflects the built adapter, ambient collectors, and instant `event.metrics` streaming; metrics-flow section distinguishes instant live stream from 5s persistence/rollup granularity), `PRODUCT.md` (Fix panel reframed as a live streaming dashboard — multi-line chart, gauges, `top`-style source table — plus a new `top`↔contextTop metric-catalog table), `METRICS.md` ("Live stream and display windows" replaces "Five-second display windows"; chart design is multi-line streaming + gauges + source table; source categories now carry fixed legend colors), `SIGNALS.md` (documented the implemented ambient collectors and that `vscode.lm.tools` includes MCP-registered tools), and `IPC.md` (documented the shipped unsequenced `event.metrics` streaming subset vs. the target sequenced contract). Doc link check green, `tsc` clean.
- [x] 2026-09-05 — **Live streaming multi-metric dashboard rewrite** — Replaced the bar-per-bucket webview with a script-based streaming dashboard (strict CSP nonce, scripts enabled): instant `postMessage` updates (no full-HTML reload = no lag), a `<canvas>` multi-line time series (total + per-source), a semantic color legend (one color per source kind, replacing the meaningless orange/purple gradient), a `top`-style sorted source table with share bars, and gauges (Peak, Rate tokens/sec, Tools/MCP, Instruction files, Open files, Terminals). Removed the "5-second window" framing per request.
- [x] 2026-09-05 — **Ambient collectors: files, instructions, tools/MCP** — Added `setupAmbientCollectors`: debounced selection (120ms), open-file scan (`files`), instruction/prompt-file scan (`copilot-instructions.md`, `*.instructions.md`, `AGENTS.md`, `*.prompt.md` → `instructions`), and `vscode.lm.tools` scan (`tools`, includes MCP-registered tools) with 5s periodic refresh; live inventory counts surfaced as gauges.
- [x] 2026-09-05 — **Fix: webview view type** — Added `"type": "webview"` to the `contextTop.fix` view contribution in package.json; without it VS Code treated the view as a TreeView and raised "There is no data provider registered that can provide view data." so the webview never resolved.
- [x] 2026-09-05 — **Fix: session-id rejection** — Extension sent `sessionId: ''` on every request; engine rejected all post-hello requests with `error.protocol`. `EngineClient.request()` now injects the authoritative session id into every payload.
- [x] 2026-09-05 — **Fix: multi-handler events** — `EngineClient.onEvent` stored one handler per type, so the provider's `event.metrics` subscription clobbered the status bar's. Changed to a handler list dispatched to all subscribers.
- [x] 2026-09-05 — **Fix: engine metrics emission** — Engine now emits `event.metrics` after each `ingestObservation` so the status bar and panel update live (was calling an undefined `send_event`).

- [x] 2026-09-05 — **Phase 1: wire-up and build (run-to-testable #6)** — Verified package.json contributions (commands, viewsContainers, views, configuration). TypeScript compiles cleanly with no errors. Cargo builds both debug and release engines. All 39 tests pass (24 lib + 15 engine). Ready for F5 launch.
- [x] 2026-09-05 — **Phase 1: panel webview live rendering (run-to-testable #5)** — ContextTopFixProvider subscribes to `event.metrics` and `event.streamGap`, renders live timeline bars and candidate breakdown by source kind. Calls `getTimeline` on initialization and after gaps. Webview disabled scripts (CSP on). `tsc` compiles cleanly, 39 tests still green.
- [x] 2026-09-05 — **Phase 1: status bar live rendering (run-to-testable #4)** — Engine.onEvent('event.metrics') handler updates status bar with `Candidate <n>k est. · Fix` label in real-time. formatTokens() helper rounds to 1k or raw count. `tsc` compiles cleanly.
- [x] 2026-09-05 — **Phase 1: editor-selection collector (run-to-testable #3)** — Added `setupSelectionMonitor()` to listen to `window.onDidChangeTextEditorSelection`, compute selection byte length, respect `contextTop.allowTransientContentProcessing` config, send `request.ingestObservation` with `sourceKind: "selection"` and ANSI-stripped transient content. `tsc` compiles cleanly.
- [x] 2026-09-05 — **Phase 1: Extension ↔ engine stdio client (run-to-testable #2)** — Implemented TypeScript `EngineClient` class spawning engine child at `target/debug/engine`, JSONL framing (1 MiB bound), nonce hello handshake with session binding, id-correlated request/response, event dispatch by type, single-restart-on-exit, graceful shutdown. Added `src/engineClient.ts` with full stdio host, integrated into extension.ts activation/deactivation. `tsc` compiles, engine binary builds.
- [x] 2026-09-05 — **Phase 1: engine read path (run-to-testable #1)** — Implement `request.getTimeline` and `request.subscribeMetrics` in-memory only; emit `response.timeline` with candidate pressure by source/kind, emit `response.subscribeMetrics` with snapshot mode. Added protocol wire types (GetTimelineRequest, TimelineResponse, MetricBucketView, SubscribeMetricsRequest/Response), engine methods (get_timeline, subscribe_metrics), subscription cursor tracking, and 2 engine tests. 24 lib + 15 engine tests green.
- [x] 2026-09-05 — **Author run-to-testable kickoff prompt (Claude Haiku)** — Wrote [`run-to-testable-prompt.md`](run-to-testable-prompt.md): ordered Phase 1 increments (engine read path → stdio client → selection collector → status bar → panel → wire-up) to a testable F5 milestone, an 80%-token-budget stop rule that keeps going until then, Haiku-friendly small-step discipline, the 9 non-negotiable rules, and verify/tracking steps.
- [x] 2026-09-05 — **Competitive evaluation + Phase 0 cleanup (docs)** — Evaluated Context Lens, ctxlens, Claude Watch, `/context`, and the VS Code Agent Debug Log/Chat Debug view against contextTop. Added the Agent Debug Log (OTLP) diagnostic sub-tier, split the diagnostics validation gate out of the removed Phase 0 prototyping phase, refreshed `ARCHITECTURE.md` status to the built engine, and threaded tools/instructions audits through the specs.
- [x] 2026-09-05 — **`Instructions` source kind** — Added `SourceKind::Instructions` (enum + wire `instructions` + round-trip test) and split instructions out of the `Prompt` bucket in `METRICS.md`/`IPC.md`/`PRODUCT.md` so instruction/prompt/agent-file overhead is a first-class, separately-accounted source.
- [x] 2026-09-05 — **Phase 1: `getRecommendations` (rank-only)** — Added the `recommend` module (per-kind ranking → `summarize_terminal`/`start_clean_chat`/`unselect_tools`/`detach_files`, savings range, executable/guided) and the engine handler with `fixId` ULID memoization by proposal-identity tuple; basis = requestId or candidate revision. No `applyFix`. 24 lib + 13 engine tests green.
- [x] 2026-09-05 — **Phase 1: `recordRequestSnapshot` + immutable request snapshot** — `build_request_snapshot` with disjoint confirmed/candidate keys, evidence-backed unknown count, confirmed-tokens-only totals, and the provenance rule (only participant/diagnostic sources can be confirmed; ambient direct_api stays candidate). Duplicate requestId → `error.protocol`; returns `requestSentSeq`.
- [x] 2026-09-05 — **Phase 1: domain model + HMAC sourceKey + tokenizer + `ingestObservation`** — Replaced prototype additive model with measurement/inclusion/coverage axes and a replace-on-update candidate gauge; added `SessionKey` HMAC-SHA-256 sourceKey derivation (lowercase hex), a fallback tokenizer registry, and the candidate-only `request.ingestObservation` handler (drops raw content).
- [x] 2026-09-05 — **Author Phase 2 kickoff prompt** — Wrote [`phase-2-prompt.md`](phase-2-prompt.md): Phase 1 preconditions, Phase 2 scope (participant `/fix`, handoff summaries, gated diagnostics adapter, tool accounting, Phase 2 settings), diagnostic-log safety rules, and a first task, grounded in `docs/SIGNALS.md` and `docs/IPC.md`.
- [x] 2026-09-05 — **Phase 1: engine skeleton + IPC hello handshake** — Added the `protocol` module (envelope + hello/error types, capability intersection, testable `accept_hello`) and a `src/bin/engine.rs` stdio host doing the nonce hello, session binding, shutdown, and bounded 1 MiB line reads per [`IPC.md`](docs/arch/IPC.md). 14 tests passing; engine binary smoke-tested end-to-end.
- [x] 2026-09-05 — **Final review pass + first-prompt handoff** — Fixed the one clear IPC doc contradiction (`usableBudgetTokens` example encoded as `null`), authored [`first-prompt.md`](first-prompt.md), and removed `review-findings.md`.

- [x] 2026-09-05 — **Independent architecture convergence review** — Run another adversarial model review, verify every finding, update documentation only, and leave Rust/TypeScript implementation untouched.
